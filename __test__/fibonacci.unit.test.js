import { isFibonacciSequence } from '../server/utils/fibonacci.js';

describe('isFibonacciSequence', () => {
  it('<UPDATE_THIS_TO_WHAT_YOU_ARE_TESTING>', () => {
    expect(isFibonacciSequence([1])).toBe(undefined);
  });
});
